# Questions 
**Q:When do we use branches?**

*A: we use branches to make sure there is no errors on the main branch or agree on which branch would be more better.*

**Q:Why do we have to `gitpull` after merging?**

*A: We use `gitpull` after merging because we have to keep our repo up to date*